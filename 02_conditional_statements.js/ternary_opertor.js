// ternaery operator :- if condition before ? is false then the function after : works otherwise not
112>23 ? console.log("hello") : console.log(hii)
console.log(343>657 ? "hehehe" : "hihihihi")

// nested turnary operator

let num = 20;
console.log(num>0 ? "positive" : num < 0 ? "negative" : "zero");
